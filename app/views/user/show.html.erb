<header class="bg-primary py-5 mb-5">
  <div class="container h-100">
    <div class="row h-100 align-items-center">
      <div class="col-lg-12">
        <h1 class="display-4 text-white mt-5 mb-2">趣味の共有結果</h1>
      </div>
    </div>
  </div>
</header>

<h2><%= Group.find(@gid).group_name %></h2>
<% @results_seikei.each do |p| %>
<%= p %><br>
<% end %>

<% if @results_seikei.empty? %>
<%= "#{Group.find(@gid).group_name}には同じ趣味を持つ人がいませんでした…"%>
まだ登録していない趣味があれば追加してみてください！
<% else %>
共有しています！
<% end %>
<hr>
<h3><%= Group.find(@gid).group_name %>に所属しているメンバー</h3>
<% @group_user_all_andme.each do |p| %>
<%= p %><br>
<% end %>
<br>
<hr>
<input type="button" class="btn btn-link" value="メンバー一覧へ戻る" onclick="location.href='/group/<%=@gid%>/list'">
<input type="button" class="btn btn-link" value="マイページへ戻る" onclick="location.href='/user/mypage/<%=@id%>'">
